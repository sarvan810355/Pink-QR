Optional decorative illustration assets go here (e.g. a mascot/empty-state
graphic). Current empty states are built from vector shapes + emoji so the
app looks complete with zero image assets — drop PNG/SVG art in here and
reference it from lib/screens if you want to upgrade them later.
